<odoo>

    <!-- ACTION -->
    <record id="action_hr_attendance_line" model="ir.actions.act_window">
        <field name="name">Attendance Punches</field>
        <field name="res_model">hr.attendance.line</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- TREE -->
    <record id="view_hr_attendance_line_tree" model="ir.ui.view">
        <field name="name">hr.attendance.line.tree</field>
        <field name="model">hr.attendance.line</field>
        <field name="arch" type="xml">
            <list>
                <field name="employee_id"/>
                <field name="punch_time"/>
                <field name="punch_state"/>
                <field name="terminal_alias"/>
                <field name="biotime_transaction_id"/>
            </list>
        </field>
    </record>

    <!-- FORM -->
    <record id="view_hr_attendance_line_form" model="ir.ui.view">
        <field name="name">hr.attendance.line.form</field>
        <field name="model">hr.attendance.line</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="employee_id"/>
                        <field name="attendance_id"/>
                    </group>
                    <group>
                        <field name="punch_time"/>
                        <field name="punch_state"/>
                    </group>
                    <group>
                        <field name="terminal_sn"/>
                        <field name="terminal_alias"/>
                        <field name="biotime_transaction_id"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

</odoo>
