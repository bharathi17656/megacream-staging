<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Inherit hr.leave form to show Advanced Approvals (container model) -->
    <record id="hr_leave_view_form_inherit_advanced" model="ir.ui.view">
        <field name="name">hr.leave.view.form.inherit.advanced</field>
        <field name="model">hr.leave</field>
        <field name="inherit_id" ref="hr_holidays.hr_leave_view_form"/>
        <field name="arch" type="xml">
            <!-- <xpath expr="//field[@name='supported_attachment_ids']" position="after">
                <separator string="Advanced Approvals" invisible="validation_type != 'advanced'"/>
                <field name="approval_advance_ids" readonly="1" context="{'default_leave_id': id}">
                    <list>
                        <field name="name"/>
                        <field name="department_id"/>
                        <field name="approver_employee_ids" widget="many2many_tags"/>
                        <field name="status"/>
                    </list>
                    <form string="Advanced Approval">
                        <group>
                            <field name="name"/>
                            <field name="department_id"/>
                            <field name="approver_employee_ids" widget="many2many_tags"/>
                            <field name="status"/>
                        </group>
                    </form>
                </field>
                
                    <separator string="Per-Approver Requests" invisible="validation_type != 'advanced'"/>
                    <field name="approval_request_ids" readonly="1" context="{'default_leave_id': id}"/>


                
            </xpath> -->

             <xpath expr="//field[@name='holiday_status_id']" position="after">
                    <field name="leave_reason" string="Reason for Leave" required="holiday_status_id in [6]" invisible="holiday_status_id not in [6]" />
                    <field name="is_special_leave" />
            </xpath>
                
        </field>
    </record>
</odoo>
